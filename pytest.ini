[pytest]
testpaths = tests
addopts = -ra --strict-markers
filterwarnings =
    error
    # The Cloud Functions target the python311 runtime, where utcnow() is
    # deprecated but present. Tracked as follow-up work; the tests run on a
    # newer interpreter than the deployed runtime.
    default:datetime.datetime.utcnow:DeprecationWarning
